import css from "./styles.module.css";

export const MAP_BACKGROUND_COLOR = {
  default: css.default,
  dark: css.dark,
  light: css.light,
};

export const MAP_TYPE = {
  wrapper: css.wrapper,
  children: css.children,
};
